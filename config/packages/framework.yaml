framework:
  secret: '%env(APP_SECRET)%'
  session: true

  csrf_protection: true

  form:
    csrf_protection:
      enabled: true
      field_name: _token

when@test:
  framework:
    test: true
    session:
      storage_factory_id: session.storage.factory.mock_file
